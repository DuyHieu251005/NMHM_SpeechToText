# Vietnamese Speech-to-Text Application Requirements
# Đồ án Nhập môn Học máy - HCMUS

# Flask web framework
flask>=2.3.0

# PyTorch - Deep Learning framework
torch>=2.0.0
torchaudio>=2.0.0

# Transformers - Hugging Face models
transformers>=4.30.0

# PEFT for LoRA adapters (PhoWhisper fine-tuning)
peft>=0.5.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# FFmpeg bundled (xử lý audio webm/ogg từ trình duyệt)
imageio-ffmpeg>=0.4.9

# Accelerate for faster inference
accelerate>=0.20.0

# Optional: for production deployment
gunicorn>=21.0.0
